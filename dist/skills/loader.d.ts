import { SkillRegistry } from '../core/skill.js';
/** Register all skills from the bundled config/skills/ directory + user dir. */
export declare function registerAllSkills(reg: SkillRegistry): void;
