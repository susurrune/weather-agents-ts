import type { ToolRegistry } from '../core/tool.js';
/** Load all plugins from the given directories into the tool registry. */
export declare function loadPlugins(reg: ToolRegistry, directories: string[]): void;
